# Postgres connection string. For local development, run `npx prisma dev`
# and copy the DATABASE_URL it prints. In production (Vercel), this is
# provided automatically once a Postgres database is attached to the project.
DATABASE_URL=""

# Shared password teachers use to log into the /teacher section.
# There is no full accounts system yet — this is a single shared password.
TEACHER_PASSWORD=""
